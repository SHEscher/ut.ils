"""
Unit test package for ut.

run with:
```shell
    pytest . --cov
    # coverage run -m pytest
    # coverage report -m
    coverage html
    open src/tests/coverage_html_report/index.html

    # Or all at once
    pytest . --cov; coverage html; open src/tests/coverage_html_report/index.html
```
"""
